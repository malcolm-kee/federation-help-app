import styles from './help.module.css';

export default function Help() {
  return (
    <div className={styles.root}>
      <h2 className={styles.title}>Time-limited offer</h2>
      <p>Apply job now to get accelerated rejection!</p>
    </div>
  );
}
